import TradingView
import math
import alerts

def get_parcel(fib_speed_resistance)
    pgf = 
    positive_cf = 
    friction = 
    negative_cf =
    parcel = [pgf, positive_cf, friction, negative_cf]
    return parcel

def tp_sl(take_profit, stop_loss)
    take_profit = 
    stop_loss = 

if __home__ = "__main__"

fib_speed_resistance = 2 #int(input())

parcel = get_parcel(fib_speed_resistance)
