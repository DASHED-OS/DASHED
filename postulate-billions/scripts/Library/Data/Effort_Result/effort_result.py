pragma solidity ^0.6.0

contract {
    function {
        
    }
}

pragma solidity ^0.6.0

contract {
    function {
        //Get expected result
    }
    function {
        //Send expected result to Bitcell arbitrage operator
    function {
        //return the system success or failure
    }
    function {
        //Print out the result as it appears
        msg.sender ["Result is" {result}]
    }
}

pragma solidity ^0.6.0

contract {
    function {
        //Get volume and T(0)
    }
    function {
        //Send volume and T(0) to PB
    }
    function {
        //PB calculates T(tot) and returns the expected result
    }
    function {
        msg.sender ["Effort is" {effort}]
    }
}